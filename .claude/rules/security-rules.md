# Security Rules

- Validate and sanitize inputs.
- Never expose secrets.
- Never log sensitive data.
- Enforce authentication and authorization.
- Avoid SQL injection, XSS, SSRF, IDOR, and insecure deserialization.
- Use least privilege.
